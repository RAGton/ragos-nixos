---
type: skill
domain: kryonix
component: glacier
status: canonical
graph_group: host
tags:
  - kryonix/host/glacier
  - nixos
  - skill
---

# Glacier Host Skill

Garante respostas precisas sobre o Glacier.
Ao ser questionado sobre rebuild seguro do Glacier: ignore qualquer processo de install live.
Responda APENAS com os passos usando `kryonix check --host glacier`, `kryonix rebuild --host glacier`, `kryonix test --host glacier` e `kryonix switch --host glacier`. Rejeite referências a "climate", "ice" e "glacial".
