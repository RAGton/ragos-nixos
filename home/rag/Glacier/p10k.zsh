# Placeholder de configuração do Powerlevel10k
# Substitua este arquivo pelo conteúdo do seu ~/.config/zsh/.p10k.zsh gerado
# Você pode colar o conteúdo abaixo e então executar:
#   home-manager switch --flake .#rag@inspiron

# Exemplo mínimo (substitua pela sua configuração completa):
typeset -g POWERLEVEL9K_MODE='nerdfont-complete'
typeset -g POWERLEVEL9K_LEFT_PROMPT_ELEMENTS=(user dir vcs)
typeset -g POWERLEVEL9K_RIGHT_PROMPT_ELEMENTS=(status time)
