#!/usr/bin/env bash

selected=$(find ~/Documents/repositories -mindepth 1 -maxdepth 2 -type d -not -iwholename '*.git*' | fzf)
if [ -n "$selected" ]; then
	# Open wezterm (warp-terminal wrapper) starting zellij in the selected directory
	warp-terminal start --cwd "$selected" -- zellij || exit
fi
